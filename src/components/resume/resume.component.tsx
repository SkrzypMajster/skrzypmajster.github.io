import React from 'react';
import './resume.component.scss';

interface IProps { }

export const ResumeComponent: React.SFC<IProps> = (props: IProps) => {
  return (
    <div>
      Resume component works.
    </div>
  );
}
