import React from 'react';
import './contact.component.scss';

interface IProps { }

export const ContactComponent: React.SFC<IProps> = (props: IProps) => {
  return (
    <div>
      Contact component works.
    </div>
  );
}
