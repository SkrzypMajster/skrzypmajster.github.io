import React from 'react';
import './projects.component.scss';

interface IProps { }

export const ProjectsComponent: React.SFC<IProps> = (props: IProps) => {
  return (
    <div>
      Projects component works.
    </div>
  );
}
